# Telegram Bot Token（从 @BotFather 获取；强烈建议不要把真实 token 提交到 git）
TELEGRAM_BOT_TOKEN=replace_me

# 允许使用的 chat_id（逗号分隔）。你可以先设置 TELEGRAM_LOG_UNKNOWN=1，
# 步骤 先让 你的 Bot 给你发一条消息（私聊 / 群里 @它）
# 浏览器打开（替换成你的 token）：
# https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
# 返回 JSON 中找：
# "chat": {
#  "id": -1001234567890,
#  "type": "supergroup",
#  "title": "test-group"
# }
# 👉 这个 id 就是你要的 chat_id
TELEGRAM_ALLOWLIST=chat_id

# 记录未在白名单的 chat_id（用于首次获取 chat_id）
TELEGRAM_LOG_UNKNOWN=0

# 隐藏内部状态行（resumed thread_id / started mode 等）
TELEGRAM_HIDE_STATUS=1

# 上传文件保存目录（相对 WORKDIR）。默认 uploads
UPLOAD_DIR=uploads

# 上传大小限制（字节）。默认 20MB
MAX_UPLOAD_BYTES=20971520

# skills 根目录（可选）
# 默认：$CODEX_HOME/skills；否则 ~/.codex/skills
# SKILLS_DIR=/Users/you/.codex/skills

# 代理配置（国内常用）
# HTTP 代理示例：http://127.0.0.1:890
# SOCKS5 代理示例：socks5://127.0.0.1:890
HTTP_PROXY=http://127.0.0.1:890
HTTPS_PROXY=http://127.0.0.1:890
NO_PROXY=127.0.0.1,localhost

# Codex CLI 启动命令
# 生产推荐：
CODEX_CMD=codex
CODEX_ARGS=

# 允许 codex 使用内置 web search（获取“最新资讯”这类需求建议开启）
CODEX_ENABLE_SEARCH=1

# driver 模式：
# - exec：使用 `codex exec --json`，更适合 Telegram（推荐）
# - interactive：尝试运行交互式 TUI（更容易遇到终端能力问题）
CODEX_DRIVER=exec

# exec 模式是否跳过 git 仓库检测（默认对 codex 为 1）
CODEX_SKIP_GIT_REPO_CHECK=1

# 工作目录（codex 的工作根目录；上传也以此为根目录）。默认：启动时当前目录
# WORKDIR=/path/to/workdir

# Go build cache（避免 macOS 默认 cache 权限问题）
GOCACHE=/tmp/gocache

# 日志目录（默认 logs；state.json 与 sessions transcript 都在这里）
LOG_DIR=logs
